# AWS SAM Step Function

### To build the step function
```bash
sam build
```

### To locally invoke just one lambda
```bash
sam local invoke GetBeersFunction
```

### To. deploy the application to AWS
```bash
sam deploy
```